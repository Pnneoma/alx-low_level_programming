#include "main.h"

/**
 * mul - Multiplies two integars
 * @x: The first number to multiply
 * @y: The second number to multiply
 * Return: Th result of multiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
